// programa para achar a raiz quadrada de um número
// a função é declarada dentro da variável
let x = function (num) { return num * num };
console.log(x(4));

// pode ser usada como valor de uma variável por outras variáveis
let y = x(3);
console.log(y);